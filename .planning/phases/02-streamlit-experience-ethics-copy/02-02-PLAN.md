---
phase: 02-streamlit-experience-ethics-copy
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app.py
  - test.py
  - tests/test_nltk_gpt2_streamlit_contract.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - QLT-03
must_haves:
  truths:
    - "NLTK and GPT-2 Streamlit entrypoints show consistent limitation and result-reminder copy"
    - "Both entrypoints show mode guidance blocks that expose launch command, speed, and memory hints"
    - "NLTK and GPT-2 result summaries include verdict confidence and analysis time in a consistent layout"
  artifacts:
    - path: "app.py"
      provides: "Normalized NLTK UI flow with shared contract copy and corrected metric display"
      contains: "build_limitations_markdown"
    - path: "test.py"
      provides: "Normalized GPT-2 UI flow with shared contract copy and mode guidance"
      contains: "build_mode_guidance_markdown"
    - path: "tests/test_nltk_gpt2_streamlit_contract.py"
      provides: "Static contract tests for NLTK/GPT-2 entrypoints"
      min_lines: 45
  key_links:
    - from: "app.py"
      to: "src/utils/ui_contract.py"
      via: "shared copy helper imports"
      pattern: "build_limitations_markdown"
    - from: "test.py"
      to: "src/utils/ui_contract.py"
      via: "shared mode guidance helper"
      pattern: "build_mode_guidance_markdown"
---

<objective>
Apply the shared UI copy contract to NLTK and GPT-2 Streamlit entrypoints, normalize summary/section flow, and add contract tests that protect UI-01, UI-02, and QLT-03 behavior.

Purpose: Ensure two primary entrypoints are consistent, launch-guided, and ethically aligned.
Output: Updated `app.py`, `test.py`, and new `tests/test_nltk_gpt2_streamlit_contract.py`.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-streamlit-experience-ethics-copy/02-CONTEXT.md
@.planning/phases/02-streamlit-experience-ethics-copy/02-RESEARCH.md
@.planning/phases/02-streamlit-experience-ethics-copy/02-UI-SPEC.md
@src/utils/ui_contract.py
@app.py
@test.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate shared UI contract into NLTK and GPT-2 apps</name>
  <files>app.py, test.py</files>
  <read_first>
    - src/utils/ui_contract.py
    - app.py
    - test.py
    - .planning/phases/02-streamlit-experience-ethics-copy/02-UI-SPEC.md
  </read_first>
  <action>
    In both `app.py` and `test.py`:
    - Import `build_limitations_markdown`, `build_result_reminder_markdown`, and `build_mode_guidance_markdown` from `src.utils.ui_contract`.
    - In sidebar, render `st.markdown(build_mode_guidance_markdown(...))` with app-specific values:
      - `app.py`: mode `NLTK`, launch `streamlit run app.py`, speed `<1s`, memory `<1 GB`
      - `test.py`: mode `GPT-2`, launch `streamlit run test.py`, speed `2-5s`, memory `2-3 GB`
    - In sidebar, render `st.markdown(build_limitations_markdown())` once below About/guide sections.
    - Near verdict display, render `st.caption(build_result_reminder_markdown())`.
    Additional normalization:
    - `app.py`: include analysis time in verdict card subtitle in same `• Analysis Time: {result.analysis_time:.2f}s` format used by other apps.
    - `app.py`: fix metrics typo `result.metrics.metrics_word_length` to `result.metrics.avg_word_length`.
  </action>
  <acceptance_criteria>
    - Both `app.py` and `test.py` contain import lines for `build_limitations_markdown` and `build_mode_guidance_markdown`.
    - Both files contain `st.markdown(build_limitations_markdown())` exactly once.
    - `app.py` contains `Analysis Time:` in verdict display and `avg_word_length` (no `metrics_word_length` remains).
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; C:/Users/Asus/AppData/Local/Programs/Python/Python310/python.exe -c "import pathlib; a=pathlib.Path('app.py').read_text(encoding='utf-8'); g=pathlib.Path('test.py').read_text(encoding='utf-8'); assert 'build_limitations_markdown' in a and 'build_limitations_markdown' in g and 'Analysis Time:' in a and 'metrics_word_length' not in a"</automated>
  </verify>
  <done>NLTK and GPT-2 apps consume shared copy contract and present normalized mode/reminder sections.</done>
</task>

<task type="auto">
  <name>Task 2: Add static entrypoint contract tests for NLTK and GPT-2 apps</name>
  <files>tests/test_nltk_gpt2_streamlit_contract.py</files>
  <read_first>
    - app.py
    - test.py
    - tests/test_nltk_analyzer.py
  </read_first>
  <action>
    Create `tests/test_nltk_gpt2_streamlit_contract.py` with static source tests that assert:
    - `app.py` contains `streamlit run app.py` and `build_mode_guidance_markdown(`.
    - `test.py` contains `streamlit run test.py` and `build_mode_guidance_markdown(`.
    - Both files contain `build_limitations_markdown()` and `build_result_reminder_markdown()`.
    - `app.py` and `test.py` each still define `st.set_page_config(` and analyze button strings (`Analyze Text` in `app.py`, `Deep Analyze with GPT-2` in `test.py`).
  </action>
  <acceptance_criteria>
    - `tests/test_nltk_gpt2_streamlit_contract.py` includes at least 4 assertions across both files.
    - `python -m pytest tests/test_nltk_gpt2_streamlit_contract.py -x -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; C:/Users/Asus/AppData/Local/Programs/Python/Python310/python.exe -m pytest tests/test_nltk_gpt2_streamlit_contract.py -x -q</automated>
  </verify>
  <done>UI-01/UI-02/QLT-03 expectations for NLTK and GPT-2 entrypoints are guarded by repeatable tests.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_nltk_gpt2_streamlit_contract.py -x` and compile-check both entry scripts.
</verification>

<success_criteria>
NLTK and GPT-2 apps follow Phase 2 UI contract with shared ethics copy, mode guidance, and consistent result summary behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/02-streamlit-experience-ethics-copy/02-02-SUMMARY.md`
</output>
