---
phase: 02-streamlit-experience-ethics-copy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/ui_contract.py
  - tests/test_ui_contract.py
autonomous: true
requirements:
  - QLT-03
  - UI-02
must_haves:
  truths:
    - "All Streamlit entrypoints can reuse the same limitation and result-reminder copy without duplicated policy text"
    - "Limitation copy contains probabilistic, English-first, and non-sole-proof statements exactly once in a shared source"
    - "UI contract helper can generate mode guidance text in a consistent format for each app"
  artifacts:
    - path: "src/utils/ui_contract.py"
      provides: "Centralized UI copy contract helpers and constants"
      contains: "LIMITATIONS_BULLETS"
    - path: "tests/test_ui_contract.py"
      provides: "Deterministic tests for copy contract and helper outputs"
      min_lines: 35
  key_links:
    - from: "src/utils/ui_contract.py"
      to: "app.py/test.py/ensemble.py"
      via: "imported helper functions"
      pattern: "from src.utils.ui_contract import"
---

<objective>
Create a shared UI copy contract module that centralizes limitations, result reminder, and mode guidance generation so Phase 2 updates across three Streamlit apps remain consistent and testable.

Purpose: Remove drift in ethics/limitations messaging and provide a reusable contract for app-level rendering.
Output: New `src/utils/ui_contract.py` and `tests/test_ui_contract.py`.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-streamlit-experience-ethics-copy/02-CONTEXT.md
@.planning/phases/02-streamlit-experience-ethics-copy/02-RESEARCH.md
@.planning/phases/02-streamlit-experience-ethics-copy/02-UI-SPEC.md
@src/utils/visualization.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared UI copy contract helpers</name>
  <files>src/utils/ui_contract.py</files>
  <read_first>
    - .planning/phases/02-streamlit-experience-ethics-copy/02-CONTEXT.md
    - .planning/phases/02-streamlit-experience-ethics-copy/02-UI-SPEC.md
    - app.py
    - test.py
    - ensemble.py
  </read_first>
  <action>
    Create `src/utils/ui_contract.py` with explicit constants and pure helper builders:
    - `LIMITATIONS_BULLETS` list with these exact strings:
      1) `Results are probabilistic indicators, not definitive proof of authorship.`
      2) `Tool behavior is English-first and may be less reliable for other languages.`
      3) `Do not use this result as sole evidence in academic or legal decisions.`
    - `RESULT_LEVEL_REMINDER` string: `Interpret this score alongside context, writing history, and human review.`
    - `def build_limitations_markdown() -> str` returning a markdown block with heading `### ⚠️ Limitations` plus the three bullets.
    - `def build_result_reminder_markdown() -> str` returning the reminder text prefixed with `ℹ️ `.
    - `def build_mode_guidance_markdown(mode_label: str, launch_command: str, speed_hint: str, memory_hint: str) -> str` returning a markdown block with heading `### 🧭 Mode Guidance` and four lines containing the passed values.
    Keep functions side-effect free (no `st.*` calls in this module).
  </action>
  <acceptance_criteria>
    - `src/utils/ui_contract.py` contains literals `Results are probabilistic indicators`, `English-first`, and `sole evidence`.
    - `src/utils/ui_contract.py` contains function definitions `build_limitations_markdown`, `build_result_reminder_markdown`, and `build_mode_guidance_markdown`.
    - `src/utils/ui_contract.py` has no `import streamlit` statement.
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; C:/Users/Asus/AppData/Local/Programs/Python/Python310/python.exe -c "from src.utils.ui_contract import LIMITATIONS_BULLETS, build_limitations_markdown; assert len(LIMITATIONS_BULLETS)==3 and 'English-first' in build_limitations_markdown()"</automated>
  </verify>
  <done>Reusable copy and guidance contract exists as pure helper code with explicit policy strings.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for UI copy contract module</name>
  <files>tests/test_ui_contract.py</files>
  <read_first>
    - src/utils/ui_contract.py
    - tests/conftest.py
    - tests/test_result_model.py
  </read_first>
  <action>
    Create `tests/test_ui_contract.py` with tests that import the contract module and assert:
    - `LIMITATIONS_BULLETS` length is 3 and each item contains expected policy tokens (`probabilistic`, `English-first`, `sole evidence`).
    - `build_limitations_markdown()` includes heading `### ⚠️ Limitations` and all three bullet lines.
    - `build_result_reminder_markdown()` contains `writing history` and starts with `ℹ️`.
    - `build_mode_guidance_markdown('NLTK', 'streamlit run app.py', '<1s', '<1 GB')` includes all provided arguments and heading `### 🧭 Mode Guidance`.
  </action>
  <acceptance_criteria>
    - `tests/test_ui_contract.py` imports from `src.utils.ui_contract` and defines at least 4 test functions.
    - `python -m pytest tests/test_ui_contract.py -x -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; C:/Users/Asus/AppData/Local/Programs/Python/Python310/python.exe -m pytest tests/test_ui_contract.py -x -q</automated>
  </verify>
  <done>Shared UI copy contract behavior is locked by fast deterministic tests.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_ui_contract.py -x` and static grep checks for required literals/functions.
</verification>

<success_criteria>
Phase 2 has a single source of truth for limitation and reminder copy, and helper behavior is test-covered for downstream app integration.
</success_criteria>

<output>
After completion, create `.planning/phases/02-streamlit-experience-ethics-copy/02-01-SUMMARY.md`
</output>
