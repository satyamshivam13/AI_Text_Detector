---
phase: 02-streamlit-experience-ethics-copy
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - ensemble.py
  - tests/test_ensemble_streamlit_contract.py
autonomous: true
requirements:
  - UI-01
  - UI-02
  - QLT-03
must_haves:
  truths:
    - "Ensemble entrypoint uses the same limitations/reminder contract as NLTK and GPT-2 apps"
    - "Ensemble mode guidance clearly states launch command and runtime/memory hints"
    - "Ensemble visualization sections preserve analyzer-specific comparison while keeping shared section order"
  artifacts:
    - path: "ensemble.py"
      provides: "Phase-2-compliant ensemble UX with shared policy copy"
      contains: "build_result_reminder_markdown"
    - path: "tests/test_ensemble_streamlit_contract.py"
      provides: "Static tests for ensemble entrypoint contract"
      min_lines: 35
  key_links:
    - from: "ensemble.py"
      to: "src/utils/ui_contract.py"
      via: "shared copy/guidance helper imports"
      pattern: "build_limitations_markdown"
---

<objective>
Integrate the shared UI copy contract into the ensemble Streamlit app and add contract tests that ensure UI-01, UI-02, and QLT-03 parity with other entrypoints while preserving ensemble-specific insights.

Purpose: Complete cross-app consistency for all documented run modes.
Output: Updated `ensemble.py` and new `tests/test_ensemble_streamlit_contract.py`.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-streamlit-experience-ethics-copy/02-CONTEXT.md
@.planning/phases/02-streamlit-experience-ethics-copy/02-RESEARCH.md
@.planning/phases/02-streamlit-experience-ethics-copy/02-UI-SPEC.md
@src/utils/ui_contract.py
@ensemble.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate shared UI contract and mode guidance into ensemble app</name>
  <files>ensemble.py</files>
  <read_first>
    - src/utils/ui_contract.py
    - ensemble.py
    - .planning/phases/02-streamlit-experience-ethics-copy/02-UI-SPEC.md
    - test.py
  </read_first>
  <action>
    Update `ensemble.py` to import and use shared helper functions:
    - Add imports for `build_limitations_markdown`, `build_result_reminder_markdown`, `build_mode_guidance_markdown`.
    - Add sidebar mode guidance block with values: mode `Ensemble`, launch `streamlit run ensemble.py`, speed `5-10s`, memory `2-3 GB`.
    - Add sidebar limitations block via `st.markdown(build_limitations_markdown())`.
    - Add verdict-adjacent reminder via `st.caption(build_result_reminder_markdown())`.
    Keep existing ensemble-specific sections (`Analyzer Comparison`, badges, component details), but keep section order consistent with UI-SPEC (summary before detailed tabs).
  </action>
  <acceptance_criteria>
    - `ensemble.py` contains calls to `build_mode_guidance_markdown`, `build_limitations_markdown`, and `build_result_reminder_markdown`.
    - `ensemble.py` still contains `Analyzer Comparison` tab label and `streamlit run ensemble.py` launch hint.
    - `ensemble.py` has one limitations block call (no duplicate contract rendering).
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; C:/Users/Asus/AppData/Local/Programs/Python/Python310/python.exe -c "import pathlib; e=pathlib.Path('ensemble.py').read_text(encoding='utf-8'); assert 'build_mode_guidance_markdown' in e and 'build_limitations_markdown' in e and 'build_result_reminder_markdown' in e and 'Analyzer Comparison' in e and 'streamlit run ensemble.py' in e"</automated>
  </verify>
  <done>Ensemble entrypoint uses shared ethics and guidance contract while preserving ensemble-specific analytics sections.</done>
</task>

<task type="auto">
  <name>Task 2: Add static ensemble entrypoint contract tests</name>
  <files>tests/test_ensemble_streamlit_contract.py</files>
  <read_first>
    - ensemble.py
    - tests/test_ensemble_analyzer.py
    - tests/test_nltk_gpt2_streamlit_contract.py
  </read_first>
  <action>
    Create `tests/test_ensemble_streamlit_contract.py` with static assertions that `ensemble.py` contains:
    - `st.set_page_config(` and analyze button text `Analyze with Ensemble`.
    - Shared helper calls: `build_mode_guidance_markdown(`, `build_limitations_markdown()`, `build_result_reminder_markdown()`.
    - Launch hint `streamlit run ensemble.py` and warning text fragments for probabilistic output.
    - Existing `Analyzer Comparison` section label.
  </action>
  <acceptance_criteria>
    - `tests/test_ensemble_streamlit_contract.py` exists with at least 4 asserts.
    - `python -m pytest tests/test_ensemble_streamlit_contract.py -x -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; C:/Users/Asus/AppData/Local/Programs/Python/Python310/python.exe -m pytest tests/test_ensemble_streamlit_contract.py -x -q</automated>
  </verify>
  <done>Ensemble UI contract is regression-covered and aligned with shared phase decisions.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_ensemble_streamlit_contract.py -x` and a compile check for `ensemble.py`.
</verification>

<success_criteria>
All documented Streamlit entrypoints have a consistent ethics/guidance contract and app-specific visualization behavior with explicit regression checks.
</success_criteria>

<output>
After completion, create `.planning/phases/02-streamlit-experience-ethics-copy/02-03-SUMMARY.md`
</output>
