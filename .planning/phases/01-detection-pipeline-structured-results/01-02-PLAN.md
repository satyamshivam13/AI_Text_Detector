---
phase: 01-detection-pipeline-structured-results
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/analyzers/ensemble_analyzer.py
autonomous: true
requirements:
  - DET-03
  - DET-04
  - QLT-02
must_haves:
  truths:
    - "Ensemble empty input matches base validation semantics and includes timing metadata on early return"
    - "Non-empty text below recommended length triggers the same recommended-length warning as BaseAnalyzer (per D-04)"
    - "Code-level documentation states default weights (RoBERTa 0.0, GPT-2 0.65, NLTK 0.35) and how fused AI score is computed"
  artifacts:
    - path: "src/analyzers/ensemble_analyzer.py"
      provides: "Parity warnings + documented weighting + explanation reflects active weights"
      contains: "recommended_text_length"
  key_links:
    - from: "EnsembleAnalyzer.analyze"
      to: "_combine_results"
      via: "normalized sub-scores times self.weights"
      pattern: "ensemble_ai_score"
---

<objective>
Align `EnsembleAnalyzer` validation and documentation with locked context decisions D-04–D-06: **recommended length** warnings, **early-return timing** (DET-04 / D-07), and **transparent weighting** in docstrings and ensemble explanation (DET-03).

Purpose: Ensemble path must not be a weaker contract than single-analyzer paths for validation or observability.
Output: Updated `ensemble_analyzer.py` only (tests in plan 01-03).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-detection-pipeline-structured-results/01-CONTEXT.md
@src/analyzers/ensemble_analyzer.py
@src/analyzers/base_analyzer.py

<interfaces>
`EnsembleAnalyzer.weights`: keys `roberta`, `gpt2`, `nltk` (defaults 0.0, 0.65, 0.35 per D-05).
`_combine_results`: builds `ensemble_ai_score` from normalized perplexity-derived AI scores.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parity for validation warnings and empty-return timing</name>
  <files>src/analyzers/ensemble_analyzer.py</files>
  <read_first>
    - src/analyzers/ensemble_analyzer.py
    - src/analyzers/base_analyzer.py
  </read_first>
  <action>
    (1) In `EnsembleAnalyzer.analyze`, after the `min_text_length` warning block, add the same `recommended_text_length` warning logic as in `BaseAnalyzer.analyze` (lines 66–70 pattern: f-string with `recommended_text_length`). (2) On empty `cleaned_text`, before return, set `result.analysis_time = round(time.time() - start_time, 3)` and `result.timestamp = time.strftime("%Y-%m-%dT%H:%M:%S")` to mirror plan 01-01 / D-07. Preserve existing warning and explanation strings for empty input.
  </action>
  <acceptance_criteria>
    - `rg -n "recommended_text_length" src/analyzers/ensemble_analyzer.py` returns at least one match inside `analyze`.
    - `rg -n "if not cleaned_text" -A25 src/analyzers/ensemble_analyzer.py` shows `analysis_time` and `timestamp` assigned before return.
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; $env:PYTHONPATH="src"; python -c "from src.analyzers.ensemble_analyzer import EnsembleAnalyzer; from src.utils.text_processing import TextProcessor; import inspect; src=inspect.getsource(EnsembleAnalyzer.analyze); assert 'recommended_text_length' in src and 'analysis_time' in src"</automated>
  </verify>
  <done>Ensemble empty and short-input validation matches base policy; empty return includes timing fields.</done>
</task>

<task type="auto">
  <name>Task 2: Document weights and surface active weights in explanation</name>
  <files>src/analyzers/ensemble_analyzer.py</files>
  <read_first>
    - src/analyzers/ensemble_analyzer.py
    - .planning/phases/01-detection-pipeline-structured-results/01-CONTEXT.md (D-05, D-06)
  </read_first>
  <action>
    Expand module or class docstring to document: default weights RoBERTa=0.0, GPT-2=0.65, NLTK=0.35; fusion formula `ensemble_ai_score = sum(weight_i * ai_score_i)` with per-backend AI scores derived from perplexity normalization in `_combine_results` (state divisor 500 used for GPT-2 and NLTK normalization). In `_generate_ensemble_explanation`, append a concise sentence listing **active** weights as percentages (e.g. multiply each weight by 100, skip or show 0% for zero weight) so callers see which backends contributed to the fused score per D-06. Keep emoji/markdown style consistent with existing explanation strings.
  </action>
  <acceptance_criteria>
    - Docstring contains literal `0.65` and `0.35` (or `65%` / `35%`) and mentions RoBERTa default 0.0.
    - `_generate_ensemble_explanation` output includes the substring `weight` or `%` near listed backend names (case-insensitive grep on a sample call is optional; prefer static string in code such as `Weights:`).
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; $env:PYTHONPATH="src"; python -c "import inspect; from src.analyzers.ensemble_analyzer import EnsembleAnalyzer; s=inspect.getsource(EnsembleAnalyzer); assert '0.65' in s or '65' in s"</automated>
  </verify>
  <done>Fusion behavior is discoverable from code; explanation reflects configured weights.</done>
</task>

</tasks>

<verification>
Grep checks above; optional manual read of `_generate_ensemble_explanation` for weight sentence.
</verification>

<success_criteria>
Ensemble validation parity with base; documented default weighting; explanation mentions active weights.
</success_criteria>

<output>
After completion, create `.planning/phases/01-detection-pipeline-structured-results/01-02-SUMMARY.md`
</output>
