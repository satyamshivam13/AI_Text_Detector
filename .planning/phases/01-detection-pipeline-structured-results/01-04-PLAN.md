---
phase: 01-detection-pipeline-structured-results
plan: 04
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - tests/test_nltk_analyzer.py
  - tests/test_gpt2_analyzer.py
  - tests/test_result_model.py
autonomous: true
requirements:
  - DET-01
  - DET-02
  - DET-04
  - QLT-02
must_haves:
  truths:
    - "NLTK and GPT-2 successful analyses expose verdict, confidence, confidence_level, metrics, explanation, analysis_time, and scores in `to_dict()`"
    - "NLTK and GPT-2 empty-input paths match QLT-02 expectations including warnings and timing fields once base early-return is fixed"
    - "Result model tests lock stable `to_dict` keys for programmatic consumers (additive-only per D-01)"
  artifacts:
    - path: "tests/test_nltk_analyzer.py"
      provides: "DET-01 structured output assertions"
    - path: "tests/test_gpt2_analyzer.py"
      provides: "DET-02 structured output assertions"
    - path: "tests/test_result_model.py"
      provides: "Canonical serialization keys"
  key_links:
    - from: "tests/test_nltk_analyzer.py"
      to: "AnalysisResult.to_dict"
      via: "assert keys after analyze(sample_ai_text)"
      pattern: "to_dict"
---

<objective>
Strengthen **NLTK** and **GPT-2** analyzer tests and **result model** tests so Phase 1 requirements DET-01, DET-02, DET-04, and QLT-02 are explicitly asserted (not merely implied). Depends on **01-01** so empty-path timing assertions are valid.

Purpose: Programmatic callers get provable contract coverage for single-analyzer paths.
Output: Extended tests only (no Streamlit changes).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@tests/test_nltk_analyzer.py
@tests/test_gpt2_analyzer.py
@tests/test_result_model.py
@src/models/result.py
@src/analyzers/nltk_analyzer.py
@src/analyzers/gpt2_analyzer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: NLTK and GPT-2 structured result and empty-path assertions</name>
  <files>tests/test_nltk_analyzer.py, tests/test_gpt2_analyzer.py</files>
  <read_first>
    - tests/test_nltk_analyzer.py
    - tests/test_gpt2_analyzer.py
    - src/models/result.py
  </read_first>
  <action>
    In `tests/test_nltk_analyzer.py`, add a test (e.g. `test_analyze_to_dict_contract`) that calls `analyze(sample_ai_text)` and asserts `to_dict()` contains keys: `verdict`, `confidence`, `confidence_level`, `perplexity`, `burstiness`, `lexical_diversity`, `sentence_variance`, `method`, `analysis_time`, `timestamp`, `text_length`, `warnings`, `explanation`, `metrics`, `scores`. Assert `analysis_time > 0` for non-empty sample. Update `test_analyze_empty_text` to assert `analysis_time >= 0` and non-empty `timestamp` string (after 01-01). Optionally assert `"Empty or invalid"` in warnings. In `tests/test_gpt2_analyzer.py`, add the same `to_dict` key test inside `TestGPT2Analysis` (marked `@pytest.mark.slow`). Extend `test_analyze_empty_text` for GPT-2 with `confidence_level == ConfidenceLevel.VERY_LOW` and timing fields like NLTK. Import `ConfidenceLevel` in GPT-2 test file if missing.
  </action>
  <acceptance_criteria>
    - `rg -n "to_dict_contract|confidence_level" tests/test_nltk_analyzer.py tests/test_gpt2_analyzer.py` shows new assertions.
    - `python -m pytest tests/test_nltk_analyzer.py -x -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; $env:PYTHONPATH="src"; python -m pytest tests/test_nltk_analyzer.py -x -q</automated>
  </verify>
  <done>NLTK path documents DET-01 + DET-04 + QLT-02 via tests; GPT-2 path has matching contract test (slow-marked).</done>
</task>

<task type="auto">
  <name>Task 2: Lock AnalysisResult.to_dict canonical keys in test_result_model</name>
  <files>tests/test_result_model.py</files>
  <read_first>
    - tests/test_result_model.py
    - src/models/result.py
  </read_first>
  <action>
    Add `test_to_dict_canonical_keys` (or similar): build a fully populated `AnalysisResult` (set `metrics` with `TextMetrics`, add one `DetectionScore`, set `warnings`, non-zero `analysis_time`, `timestamp` string). Call `to_dict()` and assert the key set **exactly equals** the expected frozenset of keys from `AnalysisResult.to_dict` implementation (list all keys: `verdict`, `confidence`, `confidence_level`, `perplexity`, `burstiness`, `lexical_diversity`, `sentence_variance`, `method`, `analysis_time`, `timestamp`, `text_length`, `warnings`, `explanation`, `metrics`, `scores`). This guards D-01 additive-only discipline — if executor adds keys intentionally, update this test in the same commit.
  </action>
  <acceptance_criteria>
    - `tests/test_result_model.py` contains `frozenset` or `set(...)` comparison against `to_dict().keys()`.
    - `python -m pytest tests/test_result_model.py -x -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; $env:PYTHONPATH="src"; python -m pytest tests/test_result_model.py -x -q</automated>
  </verify>
  <done>Serialization contract is explicitly tested for DET-04.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_nltk_analyzer.py tests/test_result_model.py -x`; run GPT-2 contract test with `-m slow` when verifying locally.
</verification>

<success_criteria>
Single-analyzer paths and `to_dict` have explicit, grep-friendly contract tests aligned with Phase 1 success criteria 1, 2, 4, 5.
</success_criteria>

<output>
After completion, create `.planning/phases/01-detection-pipeline-structured-results/01-04-SUMMARY.md`
</output>
