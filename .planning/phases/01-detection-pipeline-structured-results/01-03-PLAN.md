---
phase: 01-detection-pipeline-structured-results
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - tests/test_ensemble_analyzer.py
  - tests/test_ensemble_weighted_fusion.py
  - docs/API.md
autonomous: true
requirements:
  - DET-03
  - DET-04
  - QLT-02
must_haves:
  truths:
    - "Tests assert ensemble default weights and method name match implementation (RoBERTa 0, GPT-2 0.65, NLTK 0.35)"
    - "A unit test verifies weighted fusion math without loading transformers models"
    - "docs/API.md documents ensemble weighting and AnalysisResult fields for programmatic callers"
  artifacts:
    - path: "tests/test_ensemble_analyzer.py"
      provides: "Green tests aligned with EnsembleAnalyzer"
    - path: "tests/test_ensemble_weighted_fusion.py"
      provides: "Deterministic fusion coverage"
    - path: "docs/API.md"
      provides: "Public API and weighting narrative"
      contains: "Ensemble"
  key_links:
    - from: "tests/test_ensemble_weighted_fusion.py"
      to: "EnsembleAnalyzer._combine_results"
      via: "instance with mocked analyze outputs or direct _combine_results call"
      pattern: "_combine_results"
---

<objective>
Fix **stale ensemble tests** (they currently expect old method name and 0.45/0.35/0.20 weights). Add **deterministic fusion tests** that prove documented weighting (DET-03). Expand **docs/API.md** so ensemble behavior and structured results match code (DET-04).

Purpose: Regression safety and documented weighting behavior for Phase 1 success criteria 3 and 4.
Output: Updated ensemble tests, new fusion unit test module, expanded API doc.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/analyzers/ensemble_analyzer.py
@tests/test_ensemble_analyzer.py
@docs/API.md
@src/models/result.py

**Known mismatch:** `tests/test_ensemble_analyzer.py` expects `method_name == "Ensemble (RoBERTa+GPT2+NLTK)"` and weights `0.45/0.35/0.20`; implementation uses `"Ensemble (GPT2+NLTK)"` and `0.0/0.65/0.35`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align test_ensemble_analyzer with implementation</name>
  <files>tests/test_ensemble_analyzer.py</files>
  <read_first>
    - tests/test_ensemble_analyzer.py
    - src/analyzers/ensemble_analyzer.py
    - tests/conftest.py
  </read_first>
  <action>
    Update all assertions to match current `EnsembleAnalyzer`: `method_name`, default `weights`, `to_dict()["method"]` expectations, and any strings in `test_explanation_contains_analyzer_info` / `test_to_dict_serialization`. Keep lazy-loading tests; ensure `test_empty_text` expects `analysis_time` and `timestamp` if present after plans 01-01/01-02 (assert `analysis_time >= 0`, `timestamp` non-empty string). For `test_short_text`, assert recommended-length warning appears if text is below `recommended_text_length` (may be second warning) — use substring `accuracy` or `characters` from existing base warning text. If full ensemble `analyze` is too slow for default pytest runs, mark the heaviest tests with `@pytest.mark.slow` consistent with `test_gpt2_analyzer.py` (do not skip contract tests that only need empty/short paths without loading models — those can construct `EnsembleAnalyzer` but avoid triggering lazy load where possible; use `unittest.mock.patch` on properties `gpt2_analyzer`, `nltk_analyzer`, `roberta_analyzer` returning lightweight fake results for structural tests if needed).
  </action>
  <acceptance_criteria>
    - `python -m pytest tests/test_ensemble_analyzer.py -x -m "not slow"` passes when slow tests are marked and skipped (or full file passes if no slow mark).
    - `rg -n "0\\.45|RoBERTa\\+GPT2\\+NLTK" tests/test_ensemble_analyzer.py` returns no matches (old expectations removed).
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; $env:PYTHONPATH="src"; python -m pytest tests/test_ensemble_analyzer.py -x -q -m "not slow" 2>&1; if ($LASTEXITCODE -ne 0) { python -m pytest tests/test_ensemble_analyzer.py -x -q }</automated>
  </verify>
  <done>Ensemble test module matches code; no stale weight or method_name expectations.</done>
</task>

<task type="auto">
  <name>Task 2: Unit test weighted fusion + API documentation</name>
  <files>tests/test_ensemble_weighted_fusion.py, docs/API.md</files>
  <read_first>
    - src/analyzers/ensemble_analyzer.py
    - src/models/result.py
    - docs/API.md
  </read_first>
  <action>
    Add `tests/test_ensemble_weighted_fusion.py`. Build minimal `AnalysisResult` instances for roberta/gpt2/nltk with chosen `perplexity` values so `gpt2_ai_score` and `nltk_ai_score` follow `max(0, min(1, 1 - (ppl/500)))`; set RoBERTa score via a `DetectionScore` named containing `RoBERTa` with `value` set. Call `EnsembleAnalyzer()._combine_results(AnalysisResult(), r, g, n)` and assert `result.scores[0].value` equals expected weighted sum using default weights (0*roberta + 0.65*gpt2 + 0.35*nltk) within `1e-6`. Expand `docs/API.md`: sections for `NLTKAnalyzer`, `GPT2Analyzer`, `EnsembleAnalyzer` usage; document default ensemble weights and that RoBERTa is opt-in via weight; list `AnalysisResult` / `to_dict()` fields relevant to DET-04 (`verdict`, `confidence`, `confidence_level`, `metrics`, `explanation`, `analysis_time`, `warnings`, `scores`).
  </action>
  <acceptance_criteria>
    - `tests/test_ensemble_weighted_fusion.py` contains `pytest` test function asserting numeric fusion equals hand-computed value.
    - `docs/API.md` is at least 40 lines OR contains headings `Ensemble` and `AnalysisResult` / `to_dict`.
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; $env:PYTHONPATH="src"; python -m pytest tests/test_ensemble_weighted_fusion.py -x -q</automated>
  </verify>
  <done>DET-03 fusion is test-verified; API doc describes weights and structured output.</done>
</task>

</tasks>

<verification>
`pytest tests/test_ensemble_analyzer.py tests/test_ensemble_weighted_fusion.py -x`; skim `docs/API.md` for ensemble weights and result fields.
</verification>

<success_criteria>
Ensemble tests and docs match implementation; fusion math covered by a fast unit test.
</success_criteria>

<output>
After completion, create `.planning/phases/01-detection-pipeline-structured-results/01-03-SUMMARY.md`
</output>
