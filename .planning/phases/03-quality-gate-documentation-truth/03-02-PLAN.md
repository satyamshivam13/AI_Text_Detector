---
phase: 03-quality-gate-documentation-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - docs/API.md
  - docs/DEPLOYMENT.md
autonomous: true
requirements:
  - DOC-01
must_haves:
  truths:
    - "README, API docs, and deployment docs describe entrypoints and commands that match repository reality."
    - "Documentation clearly distinguishes core local quality gate vs optional slow verification runs."
    - "Local + Docker + docker-compose paths are accurately documented with current file names and run commands."
  artifacts:
    - path: "README.md"
      provides: "Canonical install/run/test/limitations guidance"
      contains: "streamlit run app.py"
    - path: "docs/API.md"
      provides: "Analyzer API contract and usage truth"
      contains: "AnalysisResult"
    - path: "docs/DEPLOYMENT.md"
      provides: "Local and container deployment instructions matching repo artifacts"
      contains: "docker-compose"
  key_links:
    - from: "README.md"
      to: "Makefile"
      via: "command references"
      pattern: "make test"
    - from: "docs/DEPLOYMENT.md"
      to: "Dockerfile"
      via: "runtime command truth"
      pattern: "streamlit run app.py"
---

<objective>
Align project-facing documentation with actual repository behavior for run modes, API contract, testing guidance, and deployment pathways.

Purpose: Satisfy DOC-01 with concrete file-to-file documentation truth.
Output: Updated README.md, docs/API.md, and docs/DEPLOYMENT.md.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-quality-gate-documentation-truth/03-CONTEXT.md
@.planning/phases/03-quality-gate-documentation-truth/03-RESEARCH.md
@README.md
@docs/API.md
@docs/DEPLOYMENT.md
@Makefile
@Dockerfile
@docker-compose.yml
@Procfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align README command and limitations truth with repository artifacts</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/03-quality-gate-documentation-truth/03-CONTEXT.md
    - .planning/phases/03-quality-gate-documentation-truth/03-RESEARCH.md
    - README.md
    - Makefile
    - docker-compose.yml
    - pytest.ini
  </read_first>
  <action>
    Update README command/documentation sections to match D-02 and D-03:
    - Keep documented run modes as `streamlit run app.py`, `streamlit run test.py`, and `streamlit run ensemble.py`.
    - Set `make test` as primary quality-gate command and provide Windows-safe Python equivalent directly below it.
    - Keep slow model tests documented as optional verification, not mandatory for default gate.
    - Ensure limitations text remains probabilistic, English-first, and non-sole-proof language.
    Do not change product scope, model claims, or add cloud-only requirements in README.
  </action>
  <acceptance_criteria>
    - README contains all three run commands for app.py, test.py, and ensemble.py.
    - README contains `make test` plus at least one explicit Windows-safe equivalent command.
    - README includes limitations language with the terms `probabilistic`, `English`, and `sole`.
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; C:/Users/Asus/AppData/Local/Programs/Python/Python310/python.exe -c "import pathlib; r=pathlib.Path('README.md').read_text(encoding='utf-8'); assert 'streamlit run app.py' in r and 'streamlit run test.py' in r and 'streamlit run ensemble.py' in r and 'make test' in r and 'probabilistic' in r.lower() and 'english' in r.lower() and 'sole' in r.lower()"</automated>
  </verify>
  <done>README is aligned with repo command truth and limitation messaging policy.</done>
</task>

<task type="auto">
  <name>Task 2: Align API and deployment docs to executable repository truth</name>
  <files>docs/API.md, docs/DEPLOYMENT.md</files>
  <read_first>
    - docs/API.md
    - docs/DEPLOYMENT.md
    - Makefile
    - Dockerfile
    - docker-compose.yml
    - Procfile
    - src/models/result.py
  </read_first>
  <action>
    Update docs/API.md and docs/DEPLOYMENT.md with exact repository-truth references:
    - API doc: keep analyzer usage aligned with current analyzer modules and AnalysisResult contract fields.
    - Deployment doc: ensure local run command and Docker/docker-compose sections match Dockerfile, compose service names, and Procfile command pattern.
    - Keep cloud sections intact unless a statement is clearly wrong; prioritize local + Docker + compose accuracy per D-04.
    - Add or adjust explicit command snippets when ambiguity exists.
  </action>
  <acceptance_criteria>
    - docs/API.md references AnalysisResult contract and analyzer usage examples consistent with code modules.
    - docs/DEPLOYMENT.md contains Docker build/run and docker-compose usage that matches repository files.
    - `python -m pytest tests/test_quality_gate_commands.py tests/test_nltk_gpt2_streamlit_contract.py tests/test_ensemble_streamlit_contract.py -q` passes after doc updates.
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; C:/Users/Asus/AppData/Local/Programs/Python/Python310/python.exe -m pytest tests/test_quality_gate_commands.py tests/test_nltk_gpt2_streamlit_contract.py tests/test_ensemble_streamlit_contract.py -q</automated>
  </verify>
  <done>API and deployment docs match executable repository behavior for local and container paths.</done>
</task>

</tasks>

<verification>
Run focused static tests and grep checks to confirm command/path and limitations wording consistency.
</verification>

<success_criteria>
Documentation truth is restored: README/API/deployment guidance reflects actual code entrypoints, quality-gate commands, and container artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-gate-documentation-truth/03-02-SUMMARY.md`
</output>
