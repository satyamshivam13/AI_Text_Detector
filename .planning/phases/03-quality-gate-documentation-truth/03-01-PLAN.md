---
phase: 03-quality-gate-documentation-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
  - pytest.ini
  - tests/test_quality_gate_commands.py
autonomous: true
requirements:
  - QLT-01
must_haves:
  truths:
    - "A maintainer can run one documented quality-gate command path and get deterministic pass/fail feedback."
    - "Core analyzer test suites are covered by explicit automated verification commands."
    - "Slow model tests remain optional and documented as optional."
  artifacts:
    - path: "tests/test_quality_gate_commands.py"
      provides: "Static verification of canonical test/run command references"
      min_lines: 40
    - path: "pytest.ini"
      provides: "Stable marker registration for optional slow suite handling"
      contains: "markers ="
    - path: "Makefile"
      provides: "Canonical quality-gate command target definitions"
      contains: "test:"
  key_links:
    - from: "tests/test_quality_gate_commands.py"
      to: "Makefile"
      via: "static command assertions"
      pattern: "make test"
---

<objective>
Establish a clear, reproducible Phase 3 quality-gate command contract for local verification, including optional slow-test behavior, and lock it with automated checks.

Purpose: Satisfy QLT-01 with deterministic command-level validation.
Output: Updated Makefile/pytest marker config as needed plus new quality-gate contract tests.
</objective>

<execution_context>
@$HOME/.copilot/get-shit-done/workflows/execute-plan.md
@$HOME/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-quality-gate-documentation-truth/03-CONTEXT.md
@.planning/phases/03-quality-gate-documentation-truth/03-RESEARCH.md
@.planning/codebase/TESTING.md
@Makefile
@pytest.ini
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize quality-gate command contract files</name>
  <files>Makefile, pytest.ini</files>
  <read_first>
    - .planning/phases/03-quality-gate-documentation-truth/03-CONTEXT.md
    - .planning/phases/03-quality-gate-documentation-truth/03-RESEARCH.md
    - .planning/codebase/TESTING.md
    - Makefile
    - pytest.ini
  </read_first>
  <action>
    Ensure Makefile and pytest marker config implement D-01 and D-02 exactly:
    - Keep `test` target as canonical core gate: `PYTHONPATH=src $(PYTHON) -m pytest tests/ -v --cov=src --cov-report=html --cov-report=term-missing`.
    - Ensure slow tests are optional by preserving marker registration in `pytest.ini` with clear marker text for `slow`.
    - If `pytest.ini` is missing or malformed, create/repair it with `[pytest]` and marker registration section.
    - Do not add CI-specific or cloud-specific commands in this task.
  </action>
  <acceptance_criteria>
    - Makefile contains `test:` target and the exact pytest invocation with `--cov=src`.
    - pytest.ini exists and contains `[pytest]` plus a marker entry including `slow`.
    - No new cloud/CI commands are introduced in Makefile.
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; C:/Users/Asus/AppData/Local/Programs/Python/Python310/python.exe -c "import pathlib; m=pathlib.Path('Makefile').read_text(encoding='utf-8'); p=pathlib.Path('pytest.ini').read_text(encoding='utf-8'); assert 'test:' in m and '--cov=src' in m and '[pytest]' in p and 'slow' in p"</automated>
  </verify>
  <done>Core local quality-gate command path and optional slow-test marker contract are explicit and stable.</done>
</task>

<task type="auto">
  <name>Task 2: Add static quality-gate command contract tests</name>
  <files>tests/test_quality_gate_commands.py</files>
  <read_first>
    - Makefile
    - pytest.ini
    - README.md
    - tests/test_nltk_analyzer.py
    - tests/test_gpt2_analyzer.py
    - tests/test_ensemble_analyzer.py
    - tests/test_text_processing.py
    - tests/test_result_model.py
  </read_first>
  <action>
    Create `tests/test_quality_gate_commands.py` with static assertions that:
    - Makefile test target includes pytest on `tests/` with coverage flags.
    - pytest marker configuration contains `slow` marker registration.
    - README includes canonical quality-gate command reference `make test`.
    - Core analyzer test files for NLTK, GPT-2, ensemble, text processing, and result model exist in `tests/`.
    Keep tests deterministic and file-content based only.
  </action>
  <acceptance_criteria>
    - tests/test_quality_gate_commands.py defines at least 4 test functions with explicit assertions.
    - `python -m pytest tests/test_quality_gate_commands.py -x -q` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd c:\Users\Asus\Downloads\AI_Text_Detector; C:/Users/Asus/AppData/Local/Programs/Python/Python310/python.exe -m pytest tests/test_quality_gate_commands.py -x -q</automated>
  </verify>
  <done>QLT-01 quality-gate command truth is guarded by automated static tests.</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_quality_gate_commands.py -x` and ensure Makefile/pytest marker checks pass.
</verification>

<success_criteria>
Phase 3 has a deterministic, documented, and test-guarded local quality-gate command path aligned with optional slow-test policy.
</success_criteria>

<output>
After completion, create `.planning/phases/03-quality-gate-documentation-truth/03-01-SUMMARY.md`
</output>
